# AI Research RAG Assistant — Python dependencies

# UI
streamlit>=1.35.0

# LLM & embeddings (cloud mode)
openai>=1.30.0

# Local / offline embeddings
sentence-transformers>=3.0.0

# Vector search
faiss-cpu>=1.8.0

# PDF text extraction
pypdf>=4.2.0

# HTTP client (transitive dep of openai, pinned for explicit retry handling)
httpx>=0.27.0

# Numerical operations (FAISS dependency, usually auto-installed)
numpy>=1.26.0
